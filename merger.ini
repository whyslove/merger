[uwsgi]
module = wsgi:app

enable-threads = true
single-interpreter = true
close-on-exec2 = true

processes = 1

socket = merger.sock
chmod-socket = 660
vacuum = true

die-on-term = true
for-readline = /home/recorder/.env_nvr
  env = %(_)
endfor =
